OPTICS

With and Without RangeTree

====================================

The program can be compiled as

javac *.java

It can be run as
=====================================
java Main $filename $flag $noOfPoints

$flag = 1 : with range tree
$flag = 0 : without range tree

